<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <link rel="stylesheet" href="styles.css">
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body >
    <div class="w-full min-h-screen bg-zinc-900 text-white">
        <div class="w-full flex justify-end ">
            <a class="px-3 mb-5 inline-block py-2 rounded-md bg-red-500 w-30" href="/logout">Logout</a>

        </div>
        <h3 class="text-3xl mb-5 text-blue-500">
           <span>Hello,</span> <%= user.name %>👋🏼
        </h3>
        <h5 class="mb-5">You can create a post now.</h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content" id="post" placeholder="What's on your mind?"></textarea>
            <input class="mt-2 w-45 px-3 py-2 bg-blue-500 block rounded-md" type="submit" value="Create New Post">
        </form>
        <div class="posts mt-20">
            <h3 class="text-zinc-400 mb-4">Your Posts</h3>
            <div class="postContainer mt-5">
                <% user.posts.forEach(function(post){ %>
                    <div class="post w-1/3 p-4 mb-4 rounded-md border-2 border-zinc-800 bg-zinc-800">
                        <h4 class="text-blue-500 mb-2"><%= user.username %></h4>
                        <p class="text-sm tracking-type"><%= post.content %></p>
                        <div class="btns flex gap-4 mt-5" >
                            <a class="text-blue-500" href="">Like</a>
                            <a class="text-zinc-600" href="">Edit</a>
                            <a class="text-zinc-600" href="">Delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
        </div>
  </body>
</html>
