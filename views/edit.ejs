<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full min-h-screen bg-zinc-900 text-white">
      <div class="w-full flex justify-end">
        <a
          class="px-3 mb-5 inline-block py-2 rounded-md bg-red-500 w-30"
          href="/logout"
          >Logout</a
        >
      </div>
      
      </h3>
      <h5 class="mb-5 text-blue-500">Edit Your Post</h5>
      <form action="/update/<%= post._id %>" method="post">
        <textarea
          class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
          name="content"><%= post.content %></textarea>
        <input
          class="mt-2 w-45 px-3 py-2 bg-yellow-500 text-black block rounded-md"
          type="submit"
          value="Update Post"
        />
      </form>
      
    </div>
  </body>
</html>
